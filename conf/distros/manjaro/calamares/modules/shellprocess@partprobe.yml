# Running after partition, detect new uuiid on the installDevice
---
module: shellprocess

runLocation: host

script:
    - "partprobe ${installDevice}"
    - "udevadm settle"

i18n:
    name: "partprobe..."
