# File: shellprocess@preparechroot.conf
---
dontChroot: true
script:
    - mount --bind /proc "${ROOT}/proc"
    - mount --bind /sys "${ROOT}/sys"
    - mount --bind /dev "${ROOT}/dev"
      - mount --bind /sys/firmware/efi/efivars "${ROOT}/sys/firmware/efi/efivars"
