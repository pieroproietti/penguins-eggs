Questa directory configura i default per gli utenti

In particolare per avere impostato il Desktop, servono le seguenti 
directory e sub-directory tra parentesi:

 .config (dconf)
 .cinnamon (backgrounds, configs, spices.cache)


Artisan
