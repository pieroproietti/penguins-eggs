name: _104-vagrant-alpine-3.23

# only manual, thanks
on: 
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
    - uses: actions/checkout@v6

    - name: run buidl and test
      run: |
        ./tests/104-vagrant-alpine-3.23/vagrant.install.build.test.sh

