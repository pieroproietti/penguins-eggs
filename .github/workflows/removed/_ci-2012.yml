name: _112-vagrant-linux-manjaro

on: 
  workflow_dispatch:
  
jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
    - uses: actions/checkout@v6

    - name: run buidl and test
      run: |
        ./tests/112-vagrant-linux-manjaro/vagrant.install.build.test.sh

