# =========================================================================================
# WORKFLOW DI BUILD E PUBBLICAZIONE DEL REPOSITORY APT MULTI-ARCHITETTURA
#
# Scopo:
#   Questo workflow automatizza l'intero processo di creazione e pubblicazione 
#   dei pacchetti .deb di penguins-eggs per le architetture amd64, i386 e arm64.
#
# Trigger:
#   - Push sul branch 'main'.
#   - Avvio manuale tramite 'workflow_dispatch'.
#
# Struttura:
#   1. Job 'build-deb': Compila i pacchetti .deb per tutte le architetture 
#      utilizzando un container Debian 12 pulito.
#   2. Job 'publish-repo': Scarica i pacchetti compilati, genera la struttura
#      del repository APT (indici Packages, file Release), firma crittograficamente
#      il repository con una chiave GPG e infine lo pubblica sul repository 
#      dedicato 'penguins-eggs-repo'.
#
# Prerequisiti (GitHub Secrets):
#   - GPG_SIGNING_KEY: La chiave GPG privata per firmare il repository.
#   - GPG_PASSPHRASE: La passphrase della chiave GPG.
#   - PUBLISH_TOKEN: Un Personal Access Token (PAT) con permessi di scrittura
#     sul repository 'penguins-eggs-repo'.
# =========================================================================================


name: 00. Build and Publish Debian Multi-Arch Repository

# Controllo per evitare conflitti di scrittura su gh-pages
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  # --- JOB 1: COSTRUISCE I PACCHETTI .deb PER TUTTE LE ARCHITETTURE ---
  build-deb:
    runs-on: ubuntu-latest
    # Torniamo a un container Debian pulito, non serve Ubuntu
    container:
      image: debian:12
    steps:
      - name: 1. Checkout repository code
        uses: actions/checkout@v4

      - name: 2. Install Dependencies
        # Non abilitiamo il multi-arch. Installiamo solo cio' che serve.
        run: |
          apt-get update
          apt-get install -y curl gpg git build-essential dpkg-dev fakeroot gnupg2 apt-utils xz-utils
          mkdir -p /etc/apt/keyrings
          curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg
          echo "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_20.x nodistro main" | tee /etc/apt/sources.list.d/nodesource.list
          apt-get update
          apt-get install -y nodejs
          npm install -g pnpm

      - name: 3. Import GPG Key and Prime Agent
        run: |
          echo "${{ secrets.GPG_SIGNING_KEY }}" | gpg --import --batch
          mkdir -p ~/.gnupg
          echo "pinentry-mode loopback" > ~/.gnupg/gpg.conf
          echo "allow-loopback-pinentry" > ~/.gnupg/gpg-agent.conf
          gpg-connect-agent reloadagent /bye
          echo "${{ secrets.GPG_PASSPHRASE }}" | gpg --batch --yes --passphrase-fd 0 -o /dev/null -s /dev/null

      - name: 4. Build .deb packages for all architectures
        run: |
          export GPG_TTY=$(tty)
          pnpm install
          # MODIFICA 1: Usiamo il flag '-a' per il build multi-architettura
          pnpm deb -a

      - name: 5. Upload .deb Artifact for publishing
        uses: actions/upload-artifact@v4
        with:
          name: deb-packages-for-repo
          path: releases/*


# --- JOB 2: PUBBLICA SUL REPOSITORY (DOPO IL BUILD) ---
publish-repo:
  needs: build-deb
  runs-on: ubuntu-latest
  steps:
    # MODIFICA 1: Fai il checkout del repository esistente
    # In questo modo lavoriamo sulla versione più aggiornata, senza partire da zero.
    - name: 1. Checkout existing repository (penguins-eggs-repo)
      uses: actions/checkout@v4
      with:
        repository: pieroproietti/penguins-eggs-repo
        token: ${{ secrets.PUBLISH_TOKEN }}
        # Il path dove viene clonato il repo. Lo chiamiamo 'repo-to-publish'
        # per mantenere la coerenza con il resto del workflow.
        path: repo-to-publish

    # MODIFICA 2: Scarica gli artefatti NELLA STRUTTURA ESISTENTE
    - name: 2. Download newly built artifacts
      uses: actions/download-artifact@v4
      with:
        name: deb-packages-for-repo
        # Scarichiamo i nuovi pacchetti direttamente nella cartella 'pool' esistente.
        path: repo-to-publish/deb/pool/main

    - name: 3. Install Tools and Import GPG Key
      run: |
        sudo apt-get update && sudo apt-get install -y dpkg-dev gnupg2 apt-utils xz-utils
        echo "${{ secrets.GPG_SIGNING_KEY }}" | gpg --import --batch

    # Questo step ora funziona correttamente perché opera su tutti i file (vecchi + nuovi)
    - name: 4. Update and Sign APT Repository
      run: |
        export GPG_TTY=$(tty)
        cd ./repo-to-publish/deb
        
        # ... (il resto dello script di generazione indici rimane invariato) ...
        # ... (apt-ftparchive, etc.) ...
        
        for arch in amd64 i386 arm64; do
          echo "Generating Packages index for $arch..."
          
          TEMP_REPO_DIR="temp-repo-for-$arch"
          mkdir -p "$TEMP_REPO_DIR/pool/main"
          
          # Trova i pacchetti per l'architettura specifica nella pool principale
          find pool/main -maxdepth 1 -type f -name "*_${arch}.deb" -exec cp {} "$TEMP_REPO_DIR/pool/main/" \;

          # Genera l'indice Packages basato solo sui pacchetti di quella architettura
          (cd "$TEMP_REPO_DIR" && apt-ftparchive packages .) > "dists/stable/main/binary-$arch/Packages"

          # Comprimi l'indice
          gzip -9c "dists/stable/main/binary-$arch/Packages" > "dists/stable/main/binary-$arch/Packages.gz"
          xz -9c "dists/stable/main/binary-$arch/Packages" > "dists/stable/main/binary-$arch/Packages.xz"

          # Pulisci la directory temporanea
          rm -rf "$TEMP_REPO_DIR"
        done
        
        cat > apt-release.conf <<EOF
        APT::FTPArchive::Release {
          Origin "Piero Proietti";
          Label "penguins-eggs";
          Suite "stable";
          Codename "stable";
          Architectures "amd64 i386 arm64";
          Components "main";
          Description "penguins-eggs Debian/Ubuntu repository";
        };
        EOF
        
        apt-ftparchive release -c apt-release.conf dists/stable > dists/stable/Release
        
        cd dists/stable
        mkdir -p ~/.gnupg
        echo "pinentry-mode loopback" > ~/.gnupg/gpg.conf
        echo "allow-loopback-pinentry" > ~/.gnupg/gpg-agent.conf
        gpg-connect-agent reloadagent /bye
        echo "${{ secrets.GPG_PASSPHRASE }}" | gpg --batch --yes --passphrase-fd 0 --pinentry-mode loopback --clearsign -o InRelease Release

    # MODIFICA 3: Aggiungi "keep_files: true" per evitare la cancellazione
    - name: 5. Deploy to external repository (penguins-eggs-repo)
      uses: peaceiris/actions-gh-pages@v4
      with:
        personal_token: ${{ secrets.PUBLISH_TOKEN }}
        external_repository: pieroproietti/penguins-eggs-repo
        publish_dir: ./repo-to-publish
        publish_branch: main
        # QUESTA È LA RIGA FONDAMENTALE CHE RISOLVE IL PROBLEMA
        keep_files: true
        user_name: 'github-actions[bot]'
        user_email: 'github-actions[bot]@users.noreply.github.com'
        commit_message: "Update APT repository with new packages"