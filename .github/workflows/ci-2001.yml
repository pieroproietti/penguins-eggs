name: 101-ubuntu-latest

# only manual, thanks
on: 
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
    - uses: actions/checkout@v6

    - name: run buidl and test
      run: |
        ./tests/101-ubuntu-latest/1001.ubuntu.latest.build.test.sh

