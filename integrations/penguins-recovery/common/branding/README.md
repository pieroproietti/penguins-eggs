# Branding

Shared boot menu text, splash screens, and MOTD templates for recovery images.

Place files here that should be consistent across all builder outputs:
- `motd.txt` -- Message of the day shown on login
- `grub-theme/` -- GRUB boot menu theme
- `splash.png` -- Boot splash image
