# Minimal Profile

Plasma Nano base only. Runs the recovery-launcher as a fullscreen kiosk app.
No taskbar, no system tray, no window management.

Overhead: ~50MB, ~150-300MB RAM, 3-8s boot.

## Usage

```bash
sudo make adapt INPUT=naked.iso GUI=minimal
```
