export interface IDistro {
   familyId: string
   distroId: string
   distroLike: string
   versionId: string
   versionLike: string
   packageManager: string
   isolinuxPath: string
   syslinuxPath: string
   squashFs: string
   mountpointSquashFs: string
   homeUrl: string
   supportUrl: string
   bugReportUrl: string
   calamaresAble: boolean
}
