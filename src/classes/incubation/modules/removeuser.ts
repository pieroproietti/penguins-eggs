/**
* 
*/

import Utils from '../../utils'

export function removeuser(): string {
    let text = ''
    text += '---\n'
    text += `username: live\n` // Andrebbe creata una funzione che restituisce userlive
    return text
}

