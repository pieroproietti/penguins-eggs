# Module metadata file for dummy process jobmodule
# Syntax is YAML 1.2
---
type:       "job"
name:       "remove-link"
interface:  "process"
chroot:     false
command:    "/bin/sh -c \"sleep 5 ; rm /usr/share/applications/install-debian.desktop\""
timeout:    8

