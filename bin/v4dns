#!/bin/bash
# v4dns: Ripristina i DNS di Google/Cloudflare dentro la VM Arch
echo "Raddrizzo i DNS dentro la VM Arch..."
vagrant ssh -c 'sudo rm -f /etc/resolv.conf && echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf && echo "nameserver 1.1.1.1" | sudo tee -a /etc/resolv.conf'
